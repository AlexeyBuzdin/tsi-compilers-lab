apply plugin: "groovy"

repositories {
    mavenCentral()
}

dependencies {
    compile (
        "org.codehaus.groovy:groovy-all:2.2.1",
        "org.slf4j:slf4j-log4j12:1.7.1"
    )
    testCompile (
        "org.spockframework:spock-core:0.2",
        "junit:junit:4.6"
    )
}

